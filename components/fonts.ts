import { Creepster, Ubuntu } from "next/font/google";

export const creepster = Creepster({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const ubuntu = Ubuntu({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
